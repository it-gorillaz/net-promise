const { Socket } = require("./lib/Socket"),
      { SocketError } = require("./lib/SocketError"),
      { SocketErrorType } = require("./lib/SocketErrorType");

module.exports = { Socket, SocketError, SocketErrorType };